cmake_minimum_required(VERSION 3.1)
set(Rcpp_INCLUDE_DIRS @PROJECT_SOURCE_DIR@/include)
if(WIN32)
	set(residualConnectivity_LIBRARY_DIR @PROJECT_SOURCE_DIR@/x64/)
else()
	set(Rcpp_LIBRARIES @RCPP_LIBRARY@)
endif()
